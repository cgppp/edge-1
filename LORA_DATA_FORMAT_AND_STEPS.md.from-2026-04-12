# qaLoRA 复现：项目结构与数据集格式说明

本文档说明如何在 **Qwen3 基座 + LoRA 微调** 下，用项目内脚本完成数据处理与训练，并统一使用 `**task.sh`** 与 **环境变量** 控制配置，无需手改 cnfg 文件。

---

## 零、从头学习本项目的建议路径（想看懂代码时怎么学）

若你希望**从零培养“能看懂这个项目”的能力**，可以按下面顺序来：先补一点前置概念，再按“执行流”读代码，最后跟一次训练加深印象。

### 0.1 前置知识（不必全会，够用即可）


| 主题              | 需要到什么程度                                                     | 建议                                                                                                                                                           |
| --------------- | ----------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **Python**      | 能读类、函数、`import`、字典/列表                                       | 随便一本入门书或 [Python 官方教程](https://docs.python.org/3/tutorial/) 前几章即可。                                                                                           |
| **PyTorch**     | `nn.Module`、`forward`、`state_dict`、`Parameter`、`tensor` 形状  | 官方 [60 分钟入门](https://pytorch.org/tutorials/beginner/deep_learning_60min_blitz.html)，重点“神经网络”和“自动求导”两节。                                                       |
| **Transformer** | 知道 Decoder-only：embedding → 多层（self-attn + FFN）→ norm → 输出头 | 不必手写，能看懂“输入 token id → 每层做什么 → 输出 logits”即可。可看 [The Illustrated GPT-2](https://jalammar.github.io/illustrated-gpt2/) 或《Attention Is All You Need》Decoder 部分。 |
| **LoRA**        | 公式：在 Linear 旁加低秩矩阵 A、B，只训练 A、B                              | 读 [LoRA 论文](https://arxiv.org/abs/2106.09685) 前两页或任意一篇中文 LoRA 科普，理解“冻结原权重 + 只训小矩阵”即可。                                                                        |


不必等“全部学完”再碰项目，**边看代码边查**上述概念效果更好。

---

### 0.2 本项目的推荐阅读顺序（按“执行流”走一遍）

目标：**跟着一次训练的启动过程**，从脚本入口一直跟到“模型前向 + 算 loss”。建议顺序如下。

1. **入口与配置**
  - 打开 `**task.sh`**：看它设置了哪些环境变量、最后执行哪条命令。
  - 打开 `**adv/train/plm/train_lora_qwen.py`** 的**最前面约 60 行**：看它如何用 `os.environ.get("DATA_ID", ...)` 等覆盖 `cnfg`，以及 `import cnfg.plm.qwen.v3.base as cnfg` 等从哪读配置。
2. **训练主流程（不抠细节，先看骨架）**
  - **说明**：`train_lora_qwen.py` **没有** `if __name__ == "__main__":`。主程序逻辑直接写在**模块顶层**（从约 `rid = cnfg.run_id` 到文件末尾），用 `python adv/train/plm/train_lora_qwen.py` 执行时，这些代码会按顺序运行。
  - 在 `**train_lora_qwen.py`** 里先搜索 `**def train(...)`**，看函数里如何取 batch、调用 `model(...)`、算 loss、反向传播；再看**文件后半部分**（所有函数定义之后的代码）：那里是「主程序」——读配置、建模型、load_plm、std2lora、优化器、打开 HDF5、初始验证，然后 `**for i in range(1, maxrun + 1):`** 里每轮调用 `**train(...)`**、再 eva、保存。不必先弄懂每一行，先留下“数据从哪来、loss 从哪来”的印象。
3. **模型是怎么来的**
  - 搜索 `**NMT(`** 或 `**mymodel =`**，找到**模型构建**的那一行；看它用的是哪个类（来自 `transformer.PLM.QWen.v3.Decoder` 的 `Decoder`）。
  - 打开 `**transformer/PLM/QWen/v3/Decoder.py`**：看 `**class Decoder`** 的 `**__init__**`（有哪些子模块：`wemb`、`nets`、`out_normer`、`classifier`），再看 `**forward**` 或 `**build_states**` 的大致数据流（输入 id → embed → 多层 layer → norm → logits）。
  - 看 `**load_plm**`：理解“从字典（.h5/.bin 读出来的 key-value）里按名字把 tensor 拷贝进当前模块”。
4. **LoRA 是怎么挂上去的**
  - 在 `**train_lora_qwen.py`** 里搜索 `**std2lora`**，看调用处传入的 `lora_features`、`lora_alpha` 从哪来（`cnfg.lora`）。
  - 打开 `**utils/lora/base.py`**：看 `**std2lora`** 在做什么（把 `nn.Linear` / `nn.Embedding` 替换成带 lora_wa、lora_wb 的版本）。
  - 打开 `**modules/lora/base.py**`：看 `**Linear**` 的 `**forward**`（`out = 原线性层(x) + scaling * (x @ lora_wa @ lora_wb)`），建立“只多了一小坨可训练参数”的直觉。
5. **数据与 loss**
  - 在 `**train_lora_qwen.py`** 的 `**train()`** 里看：一个 batch 的 `seq_batch`、`seq_o` 从哪来（HDF5 的 `src`、`tgt` 组）。
  - 打开 `**utils/train/llm.py`**：看 `**PMaskDataConverter`** 的 `**forward**`：输入整段 token 和 tgt 的 `[start, end]`，输出只在“回答区间”上算 loss 的 mask。这样就知道“为什么 HDF5 里 tgt 是两列”。
6. **数据是怎么生成出来的（可选）**
  - 按文档第三节的流程，从 `**scripts/plm/llm/pubdatasets_to_srctgt.py`**（原始数据 → src/tgt 文本）→ `**tools/plm/map/qwen/v3.py`**（文本 → .ids）→ `**tools/plm/mkiodata_llmdec.py**`（.ids → train.h5）走一遍，不必每行都懂，知道“每一步产出什么文件、格式是什么”即可。

按 1→2→3→4→5 走完，你就能在脑子里串起一条线：**任务脚本 → 配置 → 模型构建 → 加载基座 → 挂 LoRA → 读 HDF5 → 前向 → 只对回答区间算 loss → 反向**。

---

### 0.3 跟一次“真实训练”来巩固

- 用**小数据**（例如只保留 MetaMathQA 的一小部分）跑 1～2 个 epoch，观察日志里的 loss、`Load pre-trained model from: ...`。
- 在 `**train_lora_qwen.py`** 里对 `loss` 或 `seq_batch.shape` 加一两行 `**print`**，再跑，确认和你预期一致（例如 batch 形状、loss 是否在算）。
- 改 `**cnfg/lora.py`** 里的 `**lora_features`**（如 8→4），再跑，看参数量或日志是否有变化，加深“LoRA 只改这一处就生效”的印象。

---

### 0.4 遇到看不懂的代码时怎么查

- **某个变量/类从哪来**：在 IDE 里“跳转到定义”（F12 或 Ctrl+Click），或在整个项目里搜索该符号。
- **某个 cnfg 项在哪用**：在项目里搜索该变量名（如 `pre_trained_m`、`lora_features`），看谁在读它。
- **数据形状/格式**：看 `**utils/train/llm.py`** 和 `**utils/fmt/plm/llmdec/dual.py`**、`**tools/plm/mkiodata_llmdec.py`** 的注释或 docstring；本文档第二节也写了 HDF5 的 src/tgt 约定。
- **PyTorch / Transformer / LoRA 概念**：用上面 0.1 的链接或任意你习惯的教程查漏补缺。

把 **0.1 当作字典**，**0.2 当作主线**，**0.3 当作实验**，多走两遍就会越来越熟。

---

## 一、项目结构与关键文件

### 1.1 训练入口与配置


| 路径                                 | 说明                                                                                                                                   |
| ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| `adv/train/plm/train_lora_qwen.py` | **LoRA + Qwen 训练入口**。读 HDF5，用 `PMaskDataConverter` 只对回答部分算 loss；**脚本开头支持环境变量覆盖**：`DATA_ID`、`PRE_TRAINED_M`、`LORA_RANK`、`LORA_ALPHA`。 |
| `cnfg/base.py`                     | 全局：`data_id`、`train_data`、`dev_data`、`cache_dir`、`exp_dir`、`gpuid` 等。                                                                |
| `cnfg/plm/qwen/v3/base.py`         | Qwen 结构及 `pre_trained_m`（基座 model.h5 路径）。                                                                                            |
| `cnfg/lora.py`                     | LoRA：`lora_features`（默认 8）、`lora_alpha`（默认 16）、`prefix_ids` 等。                                                                       |
| `cnfg/hyp.py`                      | 超参：`max_tokens_gpu`、`max_sentences_gpu` 等（5090 可适当调大）。                                                                               |


### 1.2 LoRA 与数据格式


| 路径                             | 说明                                                                                     |
| ------------------------------ | -------------------------------------------------------------------------------------- |
| `modules/lora/base.py`         | LoRA 的 `Linear` / `Embedding`（lora_wa、lora_wb）。                                        |
| `utils/lora/base.py`           | `std2lora`、`lora2std`。                                                                 |
| `utils/train/llm.py`           | `**PMaskDataConverter`**：约定 HDF5 的 `src`/`tgt` 语义与形状。                                  |
| `utils/fmt/plm/llmdec/dual.py` | 双文件（指令.ids + 回答.ids）→ 拼接序列 + 每条 `[lid, lgth]`。                                         |
| `tools/plm/mkiodata_llmdec.py` | 生成 LoRA 用 HDF5（src=拼接 token 矩阵，tgt=`(batch,2)` 回答区间 `[lid, lgth]`，与 `llmdec.dual` 一致）。 |
| `tools/plm/map/qwen/v3.py`     | 文本 → 空格分隔的 token ID 行（Qwen2TokenizerFast，与 Qwen3 兼容）。                                  |
| `cnfg/vocab/plm/qwen/v3.py`    | 模板与特殊 token（`instruct_auto`、`assistant` 等）。                                            |


### 1.3 脚本与任务


| 路径                                         | 说明                                                                                                                 |
| ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------ |
| `task.sh`                                  | 激活 conda、设置 `PRE_TRAINED_M` / `DATA_ID` / `LORA_RANK` / `LORA_ALPHA`，执行 `python adv/train/plm/train_lora_qwen.py`。 |
| `scripts/plm/llm/pubdatasets_to_srctgt.py` | 从 pubdatasets（NQ/SQuAD/MetaMathQA/ToolBench）生成 src/tgt 文本；NQ 支持 `--workers` 并行。                                    |


---

### 1.4 Qwen3 + LoRA 复现相关文件树与详细说明（只关心“以 Qwen3 为基座的 LoRA”时看这里）

下面**只列出**与「Qwen3 基座 + LoRA 训练」直接相关的文件，按目录树组织，并逐项说明作用。仓库里其他文件（如 Qwen2.5、LLaMA、T5、RoBERTa 等）与本次复现无关，可忽略。

```
lora/transformer-edge/
├── task.sh                                    # 见下
├── LORA_DATA_FORMAT_AND_STEPS.md               # 本说明文档
│
├── cnfg/                                      # 配置（训练时被读取）
│   ├── base.py                                # 全局：data_id、train_data、dev_data、cache_dir、exp_dir、gpuid 等
│   ├── lora.py                                # LoRA：lora_features、lora_alpha、scaling、name_cfunc 等
│   ├── hyp.py                                 # 训练超参：max_tokens_gpu、batch 相关
│   └── plm/qwen/v3/
│       ├── base.py                            # Qwen3 结构：pre_trained_m、isize、nlayer、ff_hsize、nhead、bindDecoderEmb 等（0.6B/8B 等规模在此区分）
│       ├── hyp.py                             # Qwen3 结构选项：use_glu_ffn、use_rope、cache_len_default 等
│       ├── ihyp.py                            # 从 hyp 派生的中间量（norm、relpos 等）
│       └── __init__.py
├── cnfg/vocab/plm/qwen/v3.py                  # Qwen3 词表：vocab_size、pad_id、sos_id、eos_id、instruct 模板等
│
├── adv/train/plm/
│   └── train_lora_qwen.py                     # 【训练入口】Qwen3 Decoder + LoRA；读 DATA_ID 对应 train/dev.h5，PRE_TRAINED_M 基座，LORA_RANK/LORA_ALPHA；内部用 std2lora、PMaskDataConverter
│
├── transformer/PLM/
│   ├── NMT.py                                 # 通用 PLM 的 load_plm 包装（本项目用 Decoder 单边，不直接跑 NMT）
│   ├── LLMDecoder.py                          # 基类：embed_tokens、layers、norm、classifier、load_plm 调度各层
│   └── QWen/v3/
│       ├── Decoder.py                         # 【Qwen3 解码器】结构 + load_plm（从 .h5/.bin 按 key 灌权重的实现）
│       └── __init__.py
│
├── modules/
│   ├── lora/base.py                           # 【LoRA 核心】LoRA 版 Linear / Embedding（lora_wa、lora_wb），forward 里 base + scaling * (x @ lora_wa @ lora_wb)
│   └── plm/qwen/v3.py                         # Qwen3 的 SelfAttn、PositionwiseFF 等子模块（被 Decoder 引用）
│
├── utils/
│   ├── lora/base.py                           # std2lora：把模型里的 nn.Linear / nn.Embedding 替换成 modules.lora 的 LoRA 版；lora2std 逆操作
│   ├── train/llm.py                           # PMaskDataConverter：根据 HDF5 的 src/tgt 形状，只对“回答区间”算 loss 的 mask
│   ├── fmt/plm/llmdec/dual.py                 # 双文件 batch_loader：指令.ids + 回答.ids → 拼接序列 + [lid, lgth]，与 mkiodata_llmdec 约定一致
│   ├── plm/base.py                            # load_plm_wrapper、copy_plm_parameter、fix_parameter_name（.h5/.bin 读取与 key 映射）
│   ├── io.py                                  # save_model / h5save：保存 checkpoint 时用 ps_func 可只存 LoRA 参数（rgrad_filter）
│   └── h5serial.py                            # HDF5 读写（h5File、h5save、h5load）
│
├── tools/plm/
│   ├── convert_qwen3_hf_to_bin.py             # [新增] HuggingFace Qwen3 目录 → 单文件 .bin 或 .h5，供 PRE_TRAINED_M 使用（见第六节）
│   ├── mkiodata_llmdec.py                     # 双文件 .ids → 写入 train.h5 / dev.h5（src 组 + tgt 组，格式见第二节）
│   ├── map/qwen/v3.py                         # 文本 → 空格分隔的 token ID 行（Qwen2TokenizerFast，与 Qwen3 兼容）
│   └── token/qwen/v3.py                       # 调用 tokenizer 做 tokenize（若脚本需要）
│
└── scripts/plm/llm/
    └── pubdatasets_to_srctgt.py               # 从 pubdatasets 目录生成各数据集的 src/tgt 文本对（NQ/SQuAD/MetaMathQA/ToolBench）
```

**各层作用简述**


| 层级        | 作用                                                                                                                                                                                                         |
| --------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **入口与数据** | `task.sh` 设好环境变量后执行 `train_lora_qwen.py`；数据来自 `cache/llm/pubdatasets_*/` 下的 train.h5、dev.h5，由 `pubdatasets_to_srctgt` → map → sort → `mkiodata_llmdec` 生成。                                                 |
| **配置**    | `cnfg/base.py` 决定读哪个 data_id（进而 train_data/dev_data 路径）；`cnfg/plm/qwen/v3/base.py` 决定 Qwen3 规模与基座路径 `pre_trained_m`；`cnfg/lora.py` 决定 LoRA 的 rank/alpha。                                                   |
| **模型**    | `transformer/PLM/QWen/v3/Decoder.py` 是 Qwen3 的解码器实现；`modules/plm/qwen/v3.py` 是其中的 SelfAttn、FFN 等；`modules/lora/base.py` 是 LoRA 的 Linear/Embedding。                                                         |
| **训练逻辑**  | `train_lora_qwen.py` 里：构建 Decoder → 若 `pre_trained_m` 非空则 `load_plm` 加载基座 → `std2lora` 把部分 Linear/Embedding 换成 LoRA → 用 `PMaskDataConverter` 按 tgt 区间算 loss，只更新 LoRA 参数。                                   |
| **权重与格式** | 基座须为单文件 .h5 或 .bin（key 与 load_plm 约定一致）；若只有 HuggingFace 目录，用 `tools/plm/convert_qwen3_hf_to_bin.py` 转换。HDF5 的 src/tgt 格式由 `utils/train/llm.py` 与 `utils/fmt/plm/llmdec/dual.py`、`mkiodata_llmdec.py` 共同约定。 |


**和“复现”无关、可先忽略的目录/文件**

- `cnfg/plm/qwen/v2d5/`、`transformer/PLM/QWen/v2d5/`、`tools/plm/map/qwen/v2d5.py` 等：Qwen2.5，非 Qwen3。
- `transformer/PLM/` 下 T5、BART、LLaMa、Gemma、RoBERTa 等：其他 PLM，本复现不用。
- `adv/train/` 下非 `plm/train_lora_qwen.py` 的脚本：其他任务（GEC、prompt 等）。
- `tools/cnfg/`：若存在，多为工具用配置副本，与主流程 `cnfg/` 二选一即可。

---

## 二、LoRA 所需的数据集格式（定义在哪些文件）

- **训练脚本** 读取的 HDF5 由 `**utils/train/llm.py`** 中的 `**PMaskDataConverter`** 消费。
- **格式约定**：
  - `**src` 组**：key 为 `"0"`,`"1"`,…；value 为 `(batch_size, seq_len)` 的 token 矩阵（指令+回答拼接）。
  - `**tgt` 组**：同 key；value 为 `(batch_size, 2)`，每行 `[lid, lgth]`（`seq_batch` 上半开回答区间 `[lid, lgth)` 的端点），表示只在该区间内算 loss。
- **格式的“使用方”**：`utils/train/llm.py`（PMaskDataConverter）。
- **格式的“生成方”**：`utils/fmt/plm/llmdec/dual.py`；**写入 HDF5 的脚本**：`tools/plm/mkiodata_llmdec.py`。

### 2.1 四个数据集做这套数据处理的原因

NQ、SQuAD、MetaMathQA、ToolBench 形态不一（问答、推理、多轮对话等），但训练目标统一为：**在「指令 + 回答」的序列上，只对「回答」这一段算 loss**，这样模型学会的是“见指令后生成答案”，而不是去拟合指令里的字。因此需要：

1. **统一成「指令 → 回答」两条文本**
  四个数据集的原始字段不同（MetaMathQA 的 query/response、SQuAD 的 context+question/answers、NQ 的 question/short_answers、ToolBench 的 conversations），第一步要把它们都变成「一条指令（src）、一条回答（tgt）」的文本对，便于后面用同一套 tokenizer 和 HDF5 格式。
2. **只对回答区间算 loss**
  训练时整段序列是「指令 token + 回答 token」拼在一起；若整段都算 loss，模型会连指令里的字也去拟合。通过 **tgt 组存 `[lid, lgth]`**（与 `dual` 一致），训练脚本里的 **PMaskDataConverter** 用 `seq_o-1` 映到预测步 `ind` 上，只在回答区间上算 loss，这样梯度只驱动“生成答案”的能力。
3. **从原始表到 HDF5 的流水线**
  原始数据（parquet 等）→ 抽成 src/tgt 文本 → 用 Qwen tokenizer 打成 token ID → 按长度排序（可选，利于 batching）→ 写成项目约定的 HDF5（src 组 + tgt 组）。这样 **无论哪个数据集**，训练脚本都读同一格式的 train.h5/dev.h5，只需换 **DATA_ID** 指向不同 `pubdatasets_*` 目录即可。

**小结**：数据处理的目的 = **把四类数据统一成「指令+回答」序列，并标明回答区间，使 LoRA 训练时只对回答算 loss**；流程 = 抽文本对 → tokenize → 排序 → 写 HDF5。

### 2.2 数据处理用到的项目文件与建议路径总结

下面按 **“做一次完整数据处理”** 时你会用到的文件，按使用顺序列出，并给出**建议路径**（在项目根下执行命令时，这些路径即为你实际用到的文件）。


| 阶段                       | 目的                                  | 用到的项目文件                                                                                                                                    | 建议用法 / 路径                                                                                                                                                                                                                                                         |
| ------------------------ | ----------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **1. 原始数据 → src/tgt 文本** | 四个数据集统一成「指令一行、回答一行」                 | `**scripts/plm/llm/pubdatasets_to_srctgt.py`**                                                                                             | `python scripts/plm/llm/pubdatasets_to_srctgt.py --pubdatasets <pubdatasets根目录> --out cache/llm`；单数据集加 `--dataset metamathqa` / `nq` / `squad` / `toolbench`。输出在 `**cache/llm/pubdatasets_<名>/`**，得到 `src.train.txt`、`tgt.train.txt`、`src.dev.txt`、`tgt.dev.txt`。 |
| **2. 文本 → token ID**     | 用 Qwen tokenizer 打成 ID，与 Qwen3 基座一致 | `**tools/plm/map/qwen/v3.py`**、`**cnfg/vocab/plm/qwen/v3.py`**（模板名）                                                                        | `export TOKENIZER=/path/to/Qwen3-*`；`python tools/plm/map/qwen/v3.py $WKD/src.train.txt $TOKENIZER $WKD/src.train.txt.ids instruct_auto`（src 用 `instruct_auto`，tgt 用 `assistant`）。输出 `***.txt.ids**`。                                                             |
| **3. 按长度排序（可选）**         | 同 batch 内长度接近，减少 padding            | `**tools/sort.py`**                                                                                                                        | `python tools/sort.py $WKD/src.train.txt.ids $WKD/tgt.train.txt.ids $WKD/src.train.srt.ids $WKD/tgt.train.srt.ids 2048`。输出 `***.srt.ids`**。                                                                                                                       |
| **4. .ids → HDF5**       | 写成训练脚本能读的 src/tgt 组                 | `**tools/plm/mkiodata_llmdec.py`**；格式约定见 `**utils/fmt/plm/llmdec/dual.py`**                                                                | `python tools/plm/mkiodata_llmdec.py $WKD/src.train.srt.ids $WKD/tgt.train.srt.ids $WKD/train.h5 1`（dev 同理）。输出 `**train.h5`、`dev.h5**`，位于 `cache/llm/pubdatasets_<名>/`。                                                                                           |
| **5. 训练时读数据**            | 按 data_id 找 train/dev.h5            | `**cnfg/base.py`**（data_id、cache_dir）、`**adv/train/plm/train_lora_qwen.py`**（环境变量 DATA_ID 覆盖）、`**utils/train/llm.py**`（PMaskDataConverter） | 在 `**task.sh**` 中设 `DATA_ID=llm/pubdatasets_metamathqa`（或 nq/squad/toolbench）；训练脚本会读 `**cache_dir + DATA_ID + "/train.h5"**` 与 `**dev.h5**`。                                                                                                                      |


### 2.3 如何查看 parquet 内容与校验数据条数、内容及最终格式

从原始 parquet 到训练用 HDF5，中间会经过「src/tgt 文本 → .ids → HDF5」多步。本节说明：**如何查看 .parquet 文件内容**，以及**如何逐环节确认条数、内容与最终格式符合模型要求**。

#### 2.3.1 查看 .parquet 文件内容

**方式一：Python + pandas（推荐）**

```bash
cd /home/gpchen/lora/transformer-edge
conda activate lora-edge
python -c "
import os
from datasets import load_from_disk

# 以 MetaMathQA（save_to_disk）为例
root = os.path.expanduser('~/pubdatasets')
path = os.path.join(root, 'metamathqa')
d = load_from_disk(path)['train']
print('len:', len(d))
print('columns:', d.column_names)
print('--- sample ---')
print(d[0])
"
```

- **行数 / 列名**：`len(d)`、`d.column_names`；样本字段为 `query`、`response` 等。

**方式二：PyArrow（看 schema，不读全表）**

```python
import pyarrow.parquet as pq
t = pq.read_table("path/to/train-00000.parquet")
print(t.schema)
print(t.column_names)
print(t.num_rows)
```

**方式三：命令行（若已安装 duckdb 或 parquet-tools）**

```bash
# duckdb 示例：查看前几行
python -c "from datasets import load_from_disk; d=load_from_disk('/home/gpchen/pubdatasets/metamathqa')['train']; print(d[0])"
# 或 parquet-tools（若已安装）
parquet-tools head path/to/file.parquet
```

各数据集在 pubdatasets 中的路径与**预期列名**见 **3.1 数据集在 pubdatasets 中的布局**；脚本 `scripts/plm/llm/pubdatasets_to_srctgt.py` 按该约定读取对应列（如 MetaMathQA 的 `query`/`response`（`load_from_disk`），SQuAD 的 `context`/`question`/`answers` 等）。

#### 2.3.2 确保数据集条数在各环节一致

整条流水线中「条数」的对应关系如下，**建议在每步后做一次简单校验**。


| 环节                | 含义                                                               | 如何检查                                                                                                                                                                                                         |
| ----------------- | ---------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **1. Parquet**    | 原始表行数                                                            | `pd.read_parquet(...).shape[0]` 或合并后 `len(df)`。                                                                                                                                                              |
| **2. src/tgt 文本** | 脚本会**过滤掉「回答为空」的样本**（`filter_empty`），故条数 ≤ parquet 行数。            | `wc -l cache/llm/pubdatasets_metamathqa/src.train.txt` 与 `tgt.train.txt` 行数**必须相等**；且与脚本打印的 `train %d, dev %d` 一致。                                                                                           |
| **3. .ids 文件**    | 每行一条样本，一行 = 空格分隔的 token ID。                                      | `wc -l $WKD/src.train.txt.ids` 与 `src.train.txt` 相同；`tgt.train.txt.ids` 与 `tgt.train.txt` 相同。                                                                                                                |
| **4. HDF5**       | **ndata = batch 个数**（不是样本总条数）；每个 key `"0"`,`"1",...` 对应一个 batch。 | `python -c "import h5py; f=h5py.File('$WKD/train.h5','r'); print('ndata', f['ndata'][()]); print('batch0 src shape', f['src/0'].shape)"`。总样本数 ≈ 各 batch 的 `src/0`.shape[0] 之和（若 batch 大小不固定则需遍历 `src/i` 累加）。 |


**一键校验示例（项目根下执行）**：

```bash
export WKD=cache/llm/pubdatasets_squad_closed
echo "=== 文本行数（应相等）==="
wc -l $WKD/src.train.txt $WKD/tgt.train.txt $WKD/src.dev.txt $WKD/tgt.dev.txt
echo "=== .ids 行数（与上对应）==="
wc -l $WKD/src.train.txt.ids $WKD/tgt.train.txt.ids $WKD/src.dev.txt.ids $WKD/tgt.dev.txt.ids
echo "=== HDF5 batch 数 ==="
python -c "import h5py; f=h5py.File('$WKD/train.h5','r'); print('ndata', f['ndata'][()].item())"
```

若某步条数不一致（例如 src 与 tgt 行数不等），说明该步脚本有过滤或写错，需回到上一步排查。

#### 2.3.3 确保内容符合模型与脚本约定

- **字段**：与 **3.1** 表一致（如 MetaMathQA 必有 `query`、`response`）；若列名或结构不同，需改 `pubdatasets_to_srctgt.py` 中对应 `load_*` 的取列逻辑。
- **空回答**：脚本已过滤「tgt 为空」的样本，避免训练时「只有指令、无回答」的无效样本。
- **换行**：写入 src/tgt 文本时，脚本会把换行替换为空格（`line.replace("\n", " ").strip()`），因此**一条指令/回答在 .txt 里是一行**，与后续「一行 → 一行 .ids」对应。
- **.ids 格式**：每行为**空格分隔的整数**（token ID），与 Qwen tokenizer 输出一致；若某行含非数字或为空，`mkiodata_llmdec.py` 可能报错或产生空 batch。
- **HDF5 中 tgt 的语义**：tgt 组每行为 `**[lid, lgth]`**（与 `llmdec.dual`：回答在拼接序列上为半开 `**[lid, lgth)`**；`forward` 里 `seq_o-1` 映到 `ind` 坐标）。这与 `utils/train/llm.py` 里 **PMaskDataConverter** 的约定一致；训练时 `seq_batch` 为「指令 token + 回答 token」拼接并 pad 对齐，`seq_o` 即从 HDF5 读出的该 `(batch_size, 2)` 矩阵。

#### 2.3.4 最终数据格式与模型要求的对应关系

训练脚本（`train_lora_qwen.py`）从 HDF5 读出的是：

- **seq_batch**：`(batch_size, seq_len)`，整段 token id（指令 + 回答，已 pad 到同一长度）。
- **seq_o**：`(batch_size, 2)`，每行 `[lid, lgth]`，表示该条样本中「回答」在拼接序列里的半开区间端点（0-based）。

这两个张量交给 **PMaskDataConverter** 后：

- **oi** = `seq_batch[:, :-1]`（next-token 的输入）；
- **ot** = 只在「回答区间」位置上的目标 token（即只保留 `[start, end)` 内的下一 token）；
- **pred_mask** = 标记哪些位置参与 loss。

因此，**模型要求**可以归纳为：

1. **HDF5 src 组**：每个 key 对应一个 `(batch_size, seq_len)` 的 int 矩阵，且与 tgt 组同 key 的 `(batch_size, 2)` **行数一致**（均为同一 batch 的样本数）。
2. **HDF5 tgt 组**：每行两列、与 `dual` 一致的 `[lid, lgth]`，且满足 `0 ≤ lid < lgth ≤ seq_len`（回答区间落在序列内）。
3. **语义**：`src` 中每一行 = 一条「指令 token + 回答 token」的拼接；`tgt` 中同一行 = 该条样本中「回答」在拼接序列中的起止，训练时**只对这段区间算 loss**，与「只教模型学回答、不拟合指令」的目标一致。

只要 parquet 抽取的指令/回答正确、过滤空回答、且 **mkiodata_llmdec.py** 按 `utils/fmt/plm/llmdec/dual.py` 的约定生成 src/tgt 组与 ndata，最终 HDF5 即符合当前 LoRA 训练脚本与 PMaskDataConverter 的要求。

---

**建议路径小结**（以 MetaMathQA 为例，其他数据集只改 `pubdatasets_metamathqa` 为对应名）：

- **工作目录（每个数据集一个）**：`cache/llm/pubdatasets_metamathqa/`、`**pubdatasets_nq_closed`**、`**pubdatasets_squad_closed`**、`pubdatasets_toolbench`。`pubdatasets_to_srctgt.py` 对 **NQ / SQuAD 仅生成论文 closed-book**（与 `DATASETS_STRUCTURE_SUMMARY.md` §2.6 一致）。训练前选定并设 `WKD`/`DATA_ID`。
- **你主要要改/要设的**：环境变量 `**TOKENIZER`**（Qwen3 权重目录）、`**WKD`**（当前数据集目录）、`**DATA_ID**`（训练时用，如 `llm/pubdatasets_metamathqa`）；`**task.sh**` 里的 `**PRE_TRAINED_M**`、`**DATA_ID**`（或运行时用环境变量覆盖）。
- **不必改的文件**：`utils/fmt/plm/llmdec/dual.py`、`utils/train/llm.py`、`tools/plm/mkiodata_llmdec.py` 内部逻辑，只要按第三节命令调用即可。

---

## 三、完整流程：从 pubdatasets 到训练（统一步骤）

路径与目录以 `**/home/gpchen/pubdatasets`**、`**/home/gpchen/lora/transformer-edge`** 为准。

### 3.0 环境（建议最先做）

详见 `**docs/CONDA_SETUP.md**`。简要：

```bash
conda create -n lora-edge python=3.12 -y
conda activate lora-edge
cd /home/gpchen/lora/transformer-edge
pip install -r requirements.txt
pip install -r requirements.lora.txt
```

之后所有命令均在 `**conda activate lora-edge**` 下执行。

### 3.1 数据集在 pubdatasets 中的布局


| 数据集        | 训练                                                   | 验证/测试                                        | 字段说明                                                                                                                                             |
| ---------- | ---------------------------------------------------- | -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| MetaMathQA | `metamathqa/train/data-*.arrow`（`save_to_disk`）      | 无独立 test；脚本从 train **尾部**划 dev               | `query` → 指令，`response` → 回答                                                                                                                     |
| SQuAD      | `squad/plain_text/train-*.parquet`                   | `squad/plain_text/validation-*.parquet`      | **本脚本**：仅 **closed-book** — 仅 `question`→指令，`answers.text[0]`→回答；开卷 `context`+`question` 见 `DATASETS_STRUCTURE_SUMMARY.md` §2.5，需自行构造            |
| NQ         | `natural_questions/default/train-*-of-00287.parquet` | `natural_questions/dev/validation-*.parquet` | **本脚本**：仅 **closed-book** — 仅 `question.text`→指令，短答或 `yes_no_answer`→YES/NO；阅读式 short/long 见 §2.5 / `short.py`/`long.py`，不由本脚本导出                 |
| ToolBench  | `toolbench-v1/data/train-*.parquet`                  | `toolbench-v1/data/validation-*.parquet`     | `conversations` 多轮对话；见 [OpenBMB README_ZH](https://github.com/OpenBMB/ToolBench/blob/master/README_ZH.md) 与 `DATASETS_STRUCTURE_SUMMARY.md` §2.4 |


若使用 **MEFT 官方仓库**（`/home/gpchen/meft/MEFT`）训练而非本项目的 `src/tgt`→HDF5 流程，数据需整理为 `**instruction` / `input` / `output`** 的 `save_to_disk` 格式；**NQ** 在路径名含 `**nq_v1`** 时由脚本自动把 `question`、`long_answers` 映射到上述列，详见 `**pubdatasets/DATASETS_STRUCTURE_SUMMARY.md` §2.7** 与 MEFT 自带 `**DATASETS.md`**。

### 3.2 步骤 1：生成 src/tgt 文本

依赖：`pandas`、`pyarrow`（见 `requirements.lora.txt`）。

```bash
cd /home/gpchen/lora/transformer-edge
python scripts/plm/llm/pubdatasets_to_srctgt.py \
  --pubdatasets /home/gpchen/pubdatasets \
  --out cache/llm \
  --workers 8 \
  --dataset tool
```

- 单数据集：`--dataset metamathqa|nq|squad|toolbench`（需 `pip install datasets` 以读取 MetaMathQA）
- 限制样本：`--max-train 5000 --max-dev 500`
- **NQ / SQuAD**：脚本**仅**导出 **closed-book**（`--dataset nq` → `pubdatasets_nq_closed/`，`--dataset squad` → `pubdatasets_squad_closed/`），与 MEFT 论文设定一致；无 short/long 或开卷开关。
- NQ 加速：`--workers 8`

输出：`cache/llm/pubdatasets_metamathqa/` 等，每目录含 `src.train.txt`、`tgt.train.txt`、`src.dev.txt`、`tgt.dev.txt`。NQ 为 `**pubdatasets_nq_closed`**，SQuAD 为 `**pubdatasets_squad_closed`**。


| **目录**                     | `src.train` | `tgt.train` | `src.dev` | `tgt.dev` |
| -------------------------- | ----------- | ----------- | --------- | --------- |
| `pubdatasets_metamathqa`   | 391050      | 391050      | 3950      | 3950      |
| `pubdatasets_nq_closed`    | 110724      | 110724      | 4462      | 4462      |
| `pubdatasets_squad_closed` | 87599       | 87599       | 10570     | 10570     |
| `pubdatasets_toolbench`    | 187542      | 187542      | 762       | 762       |


### 3.3 步骤 2：文本 → token ID（map）

`TOKENIZER` 指向 **Qwen3 权重目录**（与 Qwen2 同 tokenizer；目录内需有 tokenizer 文件，勿使用文档占位路径）。

**与 `cnfg/vocab/plm/qwen/v3.py` 的关系**：`tools/plm/map/qwen/v3.py` 第 4 个参数为 **模板名**，来自该文件里的 `templated` 字典。本流程中：

- `**instruct_auto`**：把每行 **src** 包成「`user` 轮 + `assistant` 开头」，不含完整回答（与 `instruct_auto_template` 一致），供 **指令侧** 与后续和 tgt 拼接；
- `**assistant`**：把每行 **tgt** 包成「回答片段 + `</think>`」（`assistant_template`），供 **标签侧**。

四个数据集（MetaMathQA / NQ closed / SQuAD closed / ToolBench）在 3.2 中已是「一行问题/指令、一行回答」的纯文本，**map 命令相同**，**不必**按数据集改模板名；仅当你要换任务形式（如整段 `instruct_lm`、思考链 `instruct_think`、纯 LM `lm`）时才需改模板，与数据集种类无关。

可选第 5 个参数 `**system`**（默认 `You are a helpful assistant.`）：`python tools/plm/map/qwen/v3.py ... instruct_auto '自定义 system 提示'`。

```bash
export TOKENIZER=/home/common/plm/Qwen/Qwen3-8B
export WKD=cache/llm/pubdatasets_toolbench

python tools/plm/map/qwen/v3.py $WKD/src.train.txt $TOKENIZER $WKD/src.train.txt.ids instruct_auto
python tools/plm/map/qwen/v3.py $WKD/tgt.train.txt $TOKENIZER $WKD/tgt.train.txt.ids assistant
python tools/plm/map/qwen/v3.py $WKD/src.dev.txt   $TOKENIZER $WKD/src.dev.txt.ids   instruct_auto
python tools/plm/map/qwen/v3.py $WKD/tgt.dev.txt   $TOKENIZER $WKD/tgt.dev.txt.ids   assistant
```

得到 `*.txt.ids`（每行空格分隔的 token ID）。换数据集时只改 `**WKD**` 为对应 `cache/llm/pubdatasets_*`，上述四条 **模板名不变**。

### 3.4 步骤 3：按长度排序（可选）

```bash
export WKD=cache/llm/pubdatasets_toolbench
python tools/sort.py $WKD/src.train.txt.ids $WKD/tgt.train.txt.ids $WKD/src.train.srt.ids $WKD/tgt.train.srt.ids 768
python tools/sort.py $WKD/src.dev.txt.ids $WKD/tgt.dev.txt.ids $WKD/src.dev.srt.ids $WKD/tgt.dev.srt.ids 1048576
```

未排序时，下面步骤用 `.txt.ids` 替代 `.srt.ids`。

### 3.5 步骤 4：生成 LoRA 用 HDF5

```bash
export WKD=cache/llm/pubdatasets_toolbench
python tools/plm/mkiodata_llmdec.py $WKD/src.train.srt.ids $WKD/tgt.train.srt.ids $WKD/train.h5 1
python tools/plm/mkiodata_llmdec.py $WKD/src.dev.srt.ids   $WKD/tgt.dev.srt.ids   $WKD/dev.h5   1
```

若出现 “Number of batches: 0” 或报错，脚本会提示检查：.ids 文件是否存在、是否为空、每行是否为空格分隔的整数。

### 3.6 步骤 5：用 task.sh 启动训练（推荐）

四个数据集都做完 3.2～3.5 后，**统一用 `task.sh` 启动训练**，无需改 cnfg 文件。训练脚本 `**adv/train/plm/train_lora_qwen.py`** 会在开头根据环境变量覆盖：`DATA_ID`、`PRE_TRAINED_M`、`LORA_RANK`、`LORA_ALPHA`。

**前置**：基座须为项目可读的 **model.h5**（或 .bin），路径设为 `PRE_TRAINED_M`。

1. **修改 `task.sh` 中的默认值（或运行时用环境变量覆盖）**
  - `PRE_TRAINED_M`：Qwen3 的 model.h5 路径。
  - `DATA_ID`：本次使用的数据目录，与 3.2～3.5 生成的一致：
    - `llm/pubdatasets_metamathqa`
    - `llm/pubdatasets_nq_closed`
    - `llm/pubdatasets_squad_closed`
    - `llm/pubdatasets_toolbench`
2. **（可选）5090 显存**：在 `cnfg/hyp.py` 中适当调大 `max_tokens_gpu`（如 12288、16384）。
3. **执行**
  ```bash
   cd /home/gpchen/lora/transformer-edge
   bash task.sh
  ```
   或显式传参：
4. **换数据集**：改 `DATA_ID` 再执行一次即可。

**环境变量小结**：`DATA_ID`（数据目录）、`PRE_TRAINED_M`（基座路径）、`LORA_RANK`（默认 8）、`LORA_ALPHA`（默认 16）、`CUDA_VISIBLE_DEVICES`（如 0）。未设置时使用 cnfg 中的默认值。

### 3.7 单条龙示例（以 MetaMathQA 为例）

```bash
cd /home/gpchen/lora/transformer-edge
conda activate lora-edge
export TOKENIZER=/home/common/plm/Qwen/Qwen3-8B   # 改为本机 Qwen 权重目录
export WKD=cache/llm/pubdatasets_metamathqa

python scripts/plm/llm/pubdatasets_to_srctgt.py --pubdatasets /home/gpchen/pubdatasets --out cache/llm --dataset metamathqa
python tools/plm/map/qwen/v3.py $WKD/src.train.txt $TOKENIZER $WKD/src.train.txt.ids instruct_auto
python tools/plm/map/qwen/v3.py $WKD/tgt.train.txt $TOKENIZER $WKD/tgt.train.txt.ids assistant
python tools/plm/map/qwen/v3.py $WKD/src.dev.txt   $TOKENIZER $WKD/src.dev.txt.ids   instruct_auto
python tools/plm/map/qwen/v3.py $WKD/tgt.dev.txt   $TOKENIZER $WKD/tgt.dev.txt.ids   assistant
python tools/sort.py $WKD/src.train.txt.ids $WKD/tgt.train.txt.ids $WKD/src.train.srt.ids $WKD/tgt.train.srt.ids 2048
python tools/sort.py $WKD/src.dev.txt.ids $WKD/tgt.dev.txt.ids $WKD/src.dev.srt.ids $WKD/tgt.dev.srt.ids 1048576
python tools/plm/mkiodata_llmdec.py $WKD/src.train.srt.ids $WKD/tgt.train.srt.ids $WKD/train.h5 1
python tools/plm/mkiodata_llmdec.py $WKD/src.dev.srt.ids   $WKD/tgt.dev.srt.ids   $WKD/dev.h5   1

# 启动训练（任选其一）
bash task.sh
# 或：DATA_ID=llm/pubdatasets_metamathqa PRE_TRAINED_M=/path/to/Qwen3-0.6B/model.h5 python adv/train/plm/train_lora_qwen.py
```

---

## 四、以 Qwen3 为基座复现 LoRA

**第四部分的作用**：说明如何把 **Qwen3 当作基座模型（预训练权重）**，在本项目里做 LoRA 微调。包括：数据/tokenizer 是否兼容、基座权重要什么格式、`**pre_trained_m` 是什么、如何设置预训练权重**。

---

### 4.1 `pre_trained_m` 是什么？

`**pre_trained_m` 就是预训练基座权重的文件路径**（本项目里也叫「基座模型」）。

- 训练时，脚本会先按 Qwen 结构实例化一个 Decoder；若配置里 `**pre_trained_m` 不为空**，就调用 `**mymodel.load_plm(cnfg.pre_trained_m)`**，从该路径**加载已有参数**到模型里，再在此基础上加 LoRA 并只训练 LoRA 参数。
- 若 `**pre_trained_m` 为 None**，则不加载任何预训练权重，模型会**随机初始化**（一般不用于 LoRA 复现）。

因此：要做「Qwen3 + LoRA」复现，**必须**把 `pre_trained_m` 设成你的 **Qwen3 预训练权重文件** 的路径。

---

### 4.2 基座权重文件格式（.h5 或 .bin）

本项目 **只支持从单个文件加载基座**，且该文件必须是以下两种之一：


| 格式       | 说明                                          |
| -------- | ------------------------------------------- |
| **.h5**  | 项目使用的 HDF5 格式，内部是若干 key（参数名）对应 tensor 数据。   |
| **.bin** | PyTorch 的 `torch.save(state_dict)` 保存的权重字典。 |


训练脚本 **不支持** 直接读 HuggingFace 的 **safetensors** 或 **模型目录**（如 `Qwen/Qwen3-0.6B`）。若你只有 HuggingFace 下载的 Qwen3（safetensors），需要**先**用自写或第三方脚本，把权重转成上述 **.h5 或 .bin** 再使用。

---

### 4.2.1 预训练权重从哪里获取（项目内没有）

**本仓库中不包含任何预训练基座权重**（没有 .h5 / .bin 文件）。`cnfg/plm/qwen/v3/base.py` 里 `pre_trained_m = None`，`task.sh` 中的 `/path/to/Qwen3-0.6B/model.h5` 仅为占位，需自行准备并替换为实际路径。

可从以下渠道获取 Qwen3 权重，再按 4.2 的格式要求使用或转换：


| 来源               | 说明与链接                                                                                                                                                                                                    |
| ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Hugging Face** | 官方基座：**[Qwen/Qwen3-0.6B-Base](https://huggingface.co/Qwen/Qwen3-0.6B-Base)**（safetensors）。下载后需转成 .h5 或 .bin 才能被本项目加载。命令行示例：`huggingface-cli download Qwen/Qwen3-0.6B-Base --local-dir /你的目录/Qwen3-0.6B`。 |
| **ModelScope**   | 国内可用。在 [ModelScope 模型库](https://modelscope.cn/models?name=Qwen3) 搜索 Qwen3，选择对应规模（如 0.6B）下载到本地目录。同样为 safetensors，需再转换为本项目 .h5/.bin。                                                                       |
| **实验室/组内**       | 脚本中曾出现路径 `/home/common/plm/Qwen/Qwen3-0.6B`，若你所在环境有共享的 Qwen 目录或**已转好的 model.h5**，可直接向导师/同门索取路径，并在 `task.sh` 或 cnfg 中设为 `PRE_TRAINED_M` / `pre_trained_m`。                                                |


拿到的是 **safetensors 或 HF 目录** 时，必须先用自写脚本或组内工具转成 **.h5 或 .bin**，再按 4.3 设置路径；若拿到的是**已转好的 model.h5**，可直接进行 4.3 步骤 2。

---

### 4.2.2 当你有 /home/common/plm/Qwen/ 下多个 HF 模型时（选哪个、目录里有什么、如何转成本项目权重）

若你所在环境已有 `**/home/common/plm/Qwen/`**，下面通常会有多个 Qwen3 基座目录，例如：


| 目录名                | 说明                 |
| ------------------ | ------------------ |
| Qwen3-0.6B-Base    | 0.6B 预训练基座         |
| Qwen3-1.7B-Base    | 1.7B 预训练基座         |
| Qwen3-4B-Base      | 4B 预训练基座           |
| Qwen3-8B-Base      | 8B 预训练基座           |
| Qwen3-14B-Base     | 14B 预训练基座          |
| Qwen3-30B-A3B-Base | 30B-A3B MoE 预训练基座  |
| Qwen3-32B          | 32B 基座（无 -Base 后缀） |


**单个模型目录里有什么（以 Qwen3-4B-Base 为例）**  
每个这样的目录都是**标准 HuggingFace 格式**，一般包含：

- **config.json**：模型结构（层数、hidden_size、attention 等）
- **model.safetensors**：权重（单文件或分片，如 model-00001-of-00003.safetensors）
- **model.safetensors.index.json**：多分片时的权重索引
- **generation_config.json**：生成配置
- **tokenizer**：tokenizer.json、tokenizer_config.json、vocab.json、merges.txt 等

本项目**不能直接读该目录**，需要把该目录转成**单个 .bin 或 .h5 文件**后再用（见下文「如何转换」）。

**应该使用哪个模型、为什么**

- **推荐先用 Qwen3-0.6B-Base**：当前 `**cnfg/plm/qwen/v3/base.py`** 的默认超参（isize=1024、nlayer=28、ff_hsize 等）是按 **0.6B** 写的，不改配置即可训练，显存占用小、迭代快，适合先跑通 LoRA。
- 若要用 **1.7B / 4B / 8B / 14B**：必须**同时**修改 `**cnfg/plm/qwen/v3/base.py`** 中与规模对应的项（如 isize、ff_hsize、nlayer、bindDecoderEmb 等，文件内注释有各规模的取值），并保证与所选 HF 模型的 config.json 一致；否则结构对不上，加载会报错或精度异常。
- **30B-A3B、32B**：为 MoE 或更大规模，本项目默认 Decoder 为稠密结构，若要用需确认是否有对应 cnfg/实现，一般建议先用 0.6B/1.7B/4B。

**如何把 HF 目录转成本项目可用的 .bin（或 .h5）**

项目内已提供转换脚本 `**tools/plm/convert_qwen3_hf_to_bin.py`**，可将任意 HF 格式的 Qwen3 目录（如 `/home/common/plm/Qwen/Qwen3-0.6B-Base`）转为单个 **.bin** 或 **.h5** 文件，供 `PRE_TRAINED_M` 使用。

1. 在**项目根目录**下执行（示例：0.6B 转成 .bin）：
  ```bash
   python tools/plm/convert_qwen3_hf_to_bin.py /home/common/plm/Qwen/Qwen3-0.6B-Base --output /home/common/plm/Qwen/Qwen3-0.6B-Base/model.bin
  ```
2. 若想输出 **.h5**：
  ```bash
   python tools/plm/convert_qwen3_hf_to_bin.py /home/common/plm/Qwen/Qwen3-0.6B-Base --output /path/to/model.h5 --format h5
  ```
3. 转换完成后，将 `**PRE_TRAINED_M**`（或 cnfg 中的 `**pre_trained_m**`）设为上述 **model.bin** 或 **model.h5** 的路径，按 4.3 步骤 2、3 启动训练并确认日志中出现 `Load pre-trained model from: ...`。

依赖：已安装 **torch**、**transformers**；输出 .h5 时需能 import 本项目 **cnfg**（在项目根下执行即可）。

---

### 4.3 设置预训练权重的步骤（重点）

#### 步骤 1：准备好基座权重文件

- 若你**已有**别人或脚本导出的 **Qwen3 的 model.h5**（或 model.bin），记下该文件的**绝对路径**，例如：  
`/home/gpchen/plm/Qwen3-0.6B/model.h5`
- 若你**只有** HuggingFace 格式的 Qwen3 目录（如 `/home/common/plm/Qwen/Qwen3-0.6B-Base`，内含 config.json、model.safetensors、tokenizer 等）：
  1. 使用项目自带脚本转成 **.bin**（推荐）或 **.h5**：
    ```bash
     python tools/plm/convert_qwen3_hf_to_bin.py /home/common/plm/Qwen/Qwen3-0.6B-Base --output /path/to/model.bin
    ```
  2. 将 **PRE_TRAINED_M**（或 **pre_trained_m**）设为得到的 **model.bin**（或 model.h5）的路径。
  （若选用的不是 0.6B，需同时把 **`cnfg/plm/qwen/v3/base.py`** 改为对应规模的 isize、nlayer、ff_hsize 等，见 4.2.2。）

#### 步骤 2：在训练时把路径传给程序

任选一种方式即可。

**方式 A（推荐）：环境变量，配合 task.sh**

- 在 `**task.sh`** 里设置：
  ```bash
  export PRE_TRAINED_M="/home/gpchen/plm/Qwen3-0.6B/model.h5"
  ```
  或运行前在终端：
  ```bash
  export PRE_TRAINED_M=/你的路径/model.h5
  bash task.sh
  ```
- `**adv/train/plm/train_lora_qwen.py**` 开头会读 `**PRE_TRAINED_M**`，并把它赋给 `**cnfg.plm.qwen.v3.base.pre_trained_m**`，后续 `load_plm` 就会用这个路径加载预训练权重。

**方式 B：直接改配置文件**

- 打开 `**cnfg/plm/qwen/v3/base.py`**，找到：
  ```python
  pre_trained_m = None
  ```
  改为你的基座文件路径，例如：
  ```python
  pre_trained_m = "/home/gpchen/plm/Qwen3-0.6B/model.h5"
  ```
- 保存后运行 `python adv/train/plm/train_lora_qwen.py` 或 `bash task.sh`（此时可不再设 `PRE_TRAINED_M`）。

#### 步骤 3：确认训练时确实加载了基座

- 训练日志里应出现类似：`**Load pre-trained model from: /你的路径/model.h5**`。  
若没有这行且也没有报错，多半是 `**pre_trained_m` 仍为 None**（环境变量未生效或 cnfg 未改对），需要回到步骤 2 检查路径是否传入。

**小结**：预训练权重 = 基座模型文件；**设置预训练权重** = 把该文件的路径赋给 `**pre_trained_m`**（通过环境变量 `PRE_TRAINED_M` 或 cnfg 中的 `pre_trained_m`），并保证文件格式为 .h5 或 .bin。

---

### 4.4 数据与 tokenizer（与 Qwen3 的兼容性）

当前流程生成的数据（src/tgt 文本 → Qwen2TokenizerFast 打 id → train/dev.h5）**可直接用于 Qwen3 基座**。Qwen3 与 Qwen2 使用同一套 tokenizer，做 map 时把 `**TOKENIZER` 指到 Qwen3 权重目录**（与 4.3 中基座同版本即可）即可。

---

### 4.5 配置方式小结（二选一）

- **推荐**：不改 cnfg，用 `**task.sh`** 设置 `**PRE_TRAINED_M`**（及 `DATA_ID`、`LORA_RANK`、`LORA_ALPHA` 等），运行 `adv/train/plm/train_lora_qwen.py`。
- **可选**：在 `cnfg/base.py` 中设 `data_id`，在 `**cnfg/plm/qwen/v3/base.py`** 中设 `**pre_trained_m`**，在 `cnfg/lora.py` 中设 `lora_features`/`lora_alpha`，然后直接执行 `python adv/train/plm/train_lora_qwen.py`。

---

### 4.6 训练超参与 epoch、学习率等参数在哪里设置？

`adv/train/plm/train_lora_qwen.py` 从 **cnfg** 读取训练控制与优化器相关参数，**不**在脚本内写死。主要来源如下。


| 参数                              | 含义                                           | 定义位置（可直接修改的文件）                                            |
| ------------------------------- | -------------------------------------------- | --------------------------------------------------------- |
| **maxrun**                      | 最多训练多少个 epoch                                | `**cnfg/base.py`**（如 `maxrun = 128`）                      |
| **earlystop**                   | 连续多少个 epoch 验证集无提升则提前停止                      | `**cnfg/base.py`**（如 `earlystop = 8`）                     |
| **init_lr**                     | 初始学习率（优化器与学习率调度用）                            | `**cnfg/hyp.py`**（如 `init_lr = 1e-5`）                     |
| **tokens_optm**                 | 梯度累积：每多少 token 做一次 optimizer.step            | `**cnfg/base.py`**（如 `tokens_optm = 25000`）               |
| **batch_report**                | 每多少 batch 打一次日志 / 做一次验证（若 report_eva 为 True） | `**cnfg/base.py`**（如 `batch_report = 5000`）               |
| **save_every**                  | 每多少 batch 保存一次 checkpoint（None 表示不按步保存）      | `**cnfg/base.py`**（如 `save_every = 1500`）                 |
| **num_checkpoint**              | 轮转保留的 checkpoint 数量                          | `**cnfg/base.py`**（如 `num_checkpoint = 4`）                |
| **epoch_start_checkpoint_save** | 从第几个 epoch 开始按 save_every 存 checkpoint       | `**cnfg/base.py`**（如 `epoch_start_checkpoint_save = 3`）   |
| **epoch_save**                  | 是否每个 epoch 结束都存一份模型                          | `**cnfg/base.py`**（如 `epoch_save = False`）                |
| **report_eva**                  | 是否在 batch_report 时跑验证集                       | `**cnfg/base.py`**（如 `report_eva = False`）                |
| **training_steps**              | 最大训练步数（remain_steps）；若设且 ≤0 会提前结束            | `**cnfg/base.py`**（如 `training_steps = 100000`）           |
| **weight_decay**                | 优化器权重衰减                                      | `**cnfg/base.py`**（如 `weight_decay = 0`）                  |
| **warm_step** / **lr_scale**    | 学习率调度相关（当前脚本用 CustLR，未传 warm_step）           | `**cnfg/base.py`**（如 `warm_step = 8000`、`lr_scale = 1.0`） |


**说明**：`train_lora_qwen.py` 里 `import cnfg.plm.qwen.v3.base as cnfg`，且 `cnfg/plm/qwen/v3/base.py` 中有 `from cnfg.base import *`，因此 **epoch/保存/报告** 等来自 `**cnfg/base.py`**；脚本还执行 `from cnfg.plm.qwen.v3.ihyp import *`，而 ihyp 会导入 `cnfg.hyp`，故 学习率 init_lr 来自 `**cnfg/hyp.py`**。修改上述文件后重新运行脚本即可生效。

---

## 五、小结

- **LoRA 数据格式** 由 `**utils/train/llm.py`**（PMaskDataConverter）与 `**utils/fmt/plm/llmdec/dual.py`** 约定；写入 HDF5 由 `**tools/plm/mkiodata_llmdec.py**` 完成。
- **训练入口**：`**adv/train/plm/train_lora_qwen.py`**，支持环境变量 `DATA_ID`、`PRE_TRAINED_M`、`LORA_RANK`、`LORA_ALPHA`。
- **推荐使用方式**：完成 3.2～3.5 后，在 `**task.sh`** 中设好 `PRE_TRAINED_M` 与 `DATA_ID`，执行 `**bash task.sh`** 即可复现 Qwen3 + LoRA 训练。

---

## 六、本说明文档相关的新增脚本与补充注释

在 `**/home/gpchen/lora/transformer-edge`** 目录下，为配合本复现流程**新生成的**脚本与**在原有文件上补充的注释**如下。

- **新生成的 .py 脚本**（本项目内新建，仅 1 个）：`**tools/plm/convert_qwen3_hf_to_bin.py`**，添加原因见下表。

### 6.1 新生成的脚本


| 文件                                         | 添加原因                                                                                                                                                                                              |
| ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `**tools/plm/convert_qwen3_hf_to_bin.py`** | 项目训练脚本只支持从**单个 .h5 或 .bin** 加载基座，不支持直接读 HuggingFace 的 safetensors 或模型目录。本脚本将 HuggingFace 格式的 Qwen3 目录（如 `/home/common/plm/Qwen/Qwen3-0.6B-Base`）转为项目可用的 .bin 或 .h5，便于设置 `PRE_TRAINED_M`，无需手写转换逻辑。 |


### 6.2 补充了详细注释的现有文件


| 文件                                 | 添加原因                                                                                                                                                            |
| ---------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `**utils/train/llm.py`**           | 定义 **PMaskDataConverter**（只对回答区间算 loss 的核心逻辑）。补充了模块说明、类/方法 docstring、参数与返回值含义，以及 forward 内各变量与分支的注释，便于理解 HDF5 tgt 组与 loss mask 的对应关系。                           |
| `**utils/fmt/plm/llmdec/dual.py`** | 定义双文件（指令 .ids + 回答 .ids）的 batch_loader / batch_padder，与 **mkiodata_llmdec.py** 写入的 HDF5 格式一致。补充了模块说明、两函数的参数/返回值/变量含义及各代码块功能注释，便于理解「指令+回答」拼接与 `[lid, lgth]` 的由来。 |


以上脚本与注释均围绕本文档描述的复现流程与数据格式，若你修改了 HDF5 约定或基座加载方式，可相应调整或参考这些文件。

---

## 七、用其他微调方法（如 HPLSTM）替代或补充 LoRA 时，应改哪些文件、怎么改（仅文档说明，不直接改代码）

本节说明：若你想用另一种微调方式（例如 **HPLSTM**）**代替 LoRA**，或**在结构里加入 HPLSTM（例如放在注意力层之后）**，应在哪些文件中做修改、以及修改的大致思路。仅作文档指引，不直接改动代码。

### 7.1 用另一种方法（如 HPLSTM）代替 LoRA 做微调

- **主要改动的文件**：`**adv/train/plm/train_lora_qwen.py`**
- **原因**：当前 LoRA 的接入方式是「模型建好后、加载基座权重，再调用 `std2lora(mymodel, ...)` 把部分 `nn.Linear`/`nn.Embedding` 替换成 LoRA 层」。若改用其他方法（如只训练 HPLSTM、或只训练某几层），就需要在**同一段流程**里改为：不调用（或条件化）`std2lora`，改为「冻结基座、只对新增或指定的子模块解冻并参与训练」。
- **建议修改方式（概念上）**：
  1. **去掉或条件化 LoRA 分支**：约 330–342 行，当前是 `if lcnfg.lora_features is None: ... else: mymodel = std2lora(...)`。若完全不用 LoRA，可在此处不再调用 `std2lora`。
  2. **冻结基座、只训新增/指定模块**：在 `load_plm`（或 `load_model_cpu`）之后，先对整网 `mymodel.parameters()` 设 `requires_grad_(False)`，再对你希望训练的部分（例如所有 `HPLSTM` 子模块的参数）设 `requires_grad_(True)`。
  3. **保存 checkpoint 时只存可训练参数（可选）**：当前 LoRA 分支里用 `save_model_ps_func = rgrad_filter` 实现「只保存 requires_grad=True 的参数」。若你只训 HPLSTM，可同样设置一个只保存 HPLSTM（或你指定模块）参数的函数，赋给 `save_model_ps_func`，这样导出/续训时只带这些参数。

**模型结构本身**（在哪里「加入」HPLSTM）见下面 7.2。

### 7.2 把 HPLSTM 加到「注意力层之后」（或其它固定位置）

- **改哪个文件**：`**transformer/PLM/QWen/v3/Decoder.py`**（或你基于该 Decoder 的自定义子类）。
- **改哪一层 / 哪一块**：`**DecoderLayer`**（单层 Transformer block）。当前一层的数据流是：
  - `context = self.self_attn(inputo, ...)`（自注意力）
  - `context = self.ff(context)`（FFN）
  若要把 HPLSTM 放在「注意力层之后、FFN 之前」，即：**self_attn → HPLSTM → ff**，就需要在 `**DecoderLayer`** 里增加 HPLSTM 子模块，并在 `**forward`** 里在 `self_attn` 与 `ff` 之间调用它。
- **建议修改方式（概念上）**：
  1. `**DecoderLayer.__init__`**：在已有 `self.self_attn`、`self.ff` 之外，增加 `self.hplstm = HPLSTM(isize, num_head=..., ...)`（或你封装的 HPLSTM 模块）。输入/输出维度需与当前层的 `isize` 一致（或按 HPLSTM 接口设 `osize` 等）。注意：若 Decoder 使用 `share_layer=True`，所有层会共享同一个 `DecoderLayer` 实例，则「每层一个独立 HPLSTM」需要在 Decoder 的 `__init__` 里用不共享的 layer，或为每层单独注册 HPLSTM（例如放在 `Decoder` 的 `nets` 之外的另一份 `ModuleList` 里）。
  2. `**DecoderLayer.forward`**：当前为 `context = self.self_attn(...)` 后直接 `context = self.ff(context)`。在中间插入一步，例如 `context = self.hplstm(context, ...)`（若 HPLSTM 需要 `states`、`head_mask` 等，需从 `kwargs` 或上层传入，并在 `build_states`/自回归分支里维护这些状态）。
  3. **状态与 mask**：若 Decoder 有 `build_states`、自回归 `query_unit` 分支，HPLSTM 若也带状态，需要在对应分支里传入/返回 HPLSTM 的 states，与现有 `self_attn` 的 KV 状态类似地管理。

**训练脚本侧**：在 `**adv/train/plm/train_lora_qwen.py`** 中，建好模型并 `load_plm` 后，若不使用 LoRA，则不要调用 `std2lora`，并按 7.1 的方式冻结基座、仅对 HPLSTM（及你希望训练的其他模块）设 `requires_grad=True`，并可选地设置 `save_model_ps_func` 只保存这些参数。

### 7.3 小结表


| 目标                             | 主要改动的文件                                  | 改动要点（概念）                                                                                                                                                        |
| ------------------------------ | ---------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 用 HPLSTM（或其他方法）**代替 LoRA** 做微调 | `**adv/train/plm/train_lora_qwen.py`**   | 去掉或条件化 `std2lora`；冻结基座，仅对 HPLSTM（或指定模块）解冻；可选地只保存可训练参数。                                                                                                          |
| 在模型里**把 HPLSTM 加到注意力层之后**      | `**transformer/PLM/QWen/v3/Decoder.py`** | 在 `**DecoderLayer`** 的 `__init__` 中增加 `self.hplstm`，在 `**forward`** 中在 `self_attn` 与 `self.ff` 之间调用 `self.hplstm(context, ...)`；若有状态需在 `build_states`/自回归分支中传递。 |
| 训练时只训 HPLSTM、保存时只存 HPLSTM      | `**adv/train/plm/train_lora_qwen.py`**   | 同 7.1：冻结基座，仅对 HPLSTM 参数 `requires_grad=True`，并设置 `save_model_ps_func` 仅保存这些参数。                                                                                  |


### 7.4 HPLSTM 相关文件结构及作用

本小节梳理项目中与 **HPLSTM**（Head-wise Parallel LSTM 等）相关的目录与文件，便于在「用 HPLSTM 替代/补充 LoRA」或「把 HPLSTM 接到注意力层之后」时快速定位要读、要改的代码。

---

#### 7.4.1 核心模块：`modules/hplstm/`


| 文件 / 目录                       | 作用                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `**base.py`**                 | **最底层 HPLSTM 实现**。定义 `**MHPLSTMCore`**（多 head 上的 LSTM 核心：输入 + cumsum 归一化 → 门与候选 → LGate 递推 → 输出门）、`**HPLSTM`**（`trans_input` → MHPLSTMCore → `trans_output`，输入/输出形状 (batch, seq, isize)）、`**BiHPLSTM**`（双向，需 `mask`/`pad_reversed_mask`）、`**ResHPLSTM**`（output = HPLSTM(x) + x）、`**ResBiHPLSTM**`。若要在 Qwen DecoderLayer 里「注意力之后」接一个 HPLSTM，通常从这里用 `**HPLSTM**` 或 `**ResHPLSTM**`（接口：`forward(inpute, states=None, head_mask=None)`）。 |
| `**snbase.py**`               | 在 `base` 之上换用 **RS1cumsumstatnorm**（`utils/hplstm`）和 **LGate** 的 Core 变体，并导出 **ResHPLSTM** 等。部分 **trans_og** 使用 `GroupLinear` 而非带 LayerNorm 的 Sequential。被 `**transformer/HPLSTM/`**、`**transformer/PLM/HPLSTM/v1/`** 等引用。                                                                                                                                                                                                            |
| `**snogi.py**`、`**snogc.py**` | 继承 `snbase` 的 MHPLSTMCore / HPLSTM / ResHPLSTM，换用不同底层算子或配置（如另一套 LGate/RS1cumsumstatnorm 组合），用于实验或特定任务。                                                                                                                                                                                                                                                                                                                                |
| `**hfn.py**`                  | 带 **fhsize**、可选 **GLU**、更复杂前馈的 **MHPLSTMCore** 变体，用于 `**transformer/HPLSTM/FNDecoder.py`**、**FNEncoder.py**、**FNLM.py** 等「前馈式」HPLSTM 结构。                                                                                                                                                                                                                                                                                              |
| `**wrapper.py`**              | 对 HPLSTM 的封装（如 **LSTM4RNMT**），供 `modules/timeseries.py` 等使用。                                                                                                                                                                                                                                                                                                                                                                          |
| `**mem/`**（含 `mem/soft/`）     | 带记忆/软注意的 HPLSTM 变体（如 **masnbase.py**、**snbase.py**），用于需要额外记忆模块的场景。                                                                                                                                                                                                                                                                                                                                                                    |


**接入 Qwen 时**：优先看 `**modules/hplstm/base.py`** 的 `**HPLSTM`**、`**ResHPLSTM`** 的 `__init__`（`isize`、`num_head`、`osize`、`act_drop` 等）和 `**forward(inpute, states=None, head_mask=None)**`，确认输入/输出维度和 states 的传递方式。

---

#### 7.4.2 底层算子与工具：`utils/hplstm/`


| 文件                                                           | 作用                                                                                                                                                   |
| ------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| `**LGate.py**`                                               | **LGateFunc**：按序列维做「遗忘门 × 上一 cell + 输入门 × 候选」的递推（LSTM 的 cell 更新），有 CPU（lgatev_cpp）与 CUDA（lgates_cuda）实现。被 `modules/hplstm/base.py` 的 MHPLSTMCore 调用。 |
| `**RS1cumsumstatnorm.py`**                                   | **RS1cumsumstatnorm**：对多 head 输入做 cumsum（或带 state 的递推）、再归一化，供 MHPLSTMCore 的「csum」分支。支持 **states** 与 **csum_state_return**，用于自回归时跨步传递。                |
| `**RS1cumsum.py`**、`**RS1cumsumstat.py`**                    | 累积和及其与统计量的组合，被 RS1cumsumstatnorm 等调用；可选 C++/CUDA 扩展（`utils/cpp/hplstm/`）。                                                                            |
| `**MvAvg.py**`、`**RS1MvAvg.py**`、`**RS1mvavgstatnorm.py**` 等 | 移动平均类实现，与 cumsum 并列的另一类「序列聚合」，用于部分 HPLSTM 变体。                                                                                                        |
| `**ilen.py**`                                                | 与序列长度、mask 相关的工具（如 **H2TiLentoMask**、**head2tail**），被 `**transformer/PLM/HPLSTM/v1/Decoder.py`** 等使用。                                                |
| `**test/`**                                                  | 对 LGate、cumsum、MvAvg 等算子的单元/脚本测试。                                                                                                                    |


理解 **LGate** 与 **RS1cumsumstatnorm** 的输入/输出和 **states** 约定，有助于在 Qwen 的 `build_states`/自回归分支里正确传递 HPLSTM 状态。

---

#### 7.4.3 C++ / CUDA 扩展：`utils/cpp/hplstm/`


| 文件                                                                                             | 作用                                                      |
| ---------------------------------------------------------------------------------------------- | ------------------------------------------------------- |
| `**lgatev.cpp`**、`**lgatev_nocx.cpp`**                                                         | LGate 的 CPU 实现（单步/无 context 等变体）。                       |
| `**lgates.cpp**`、`**lgates_cuda.cpp**`、`**lgates_cuda_kernel.cu**`                             | LGate 的序列维递推（CPU OpenMP / CUDA）。                        |
| `**clcumsum.cpp**`、`**ml2cumsums_cuda.cpp**`（及 `*_kernel.cu`）                                  | 累积和的 C++/CUDA 实现，供 **RS1cumsum**、**RS1cumsumstat** 等调用。 |
| `**clmvavg.cpp`**、`**clmvavgis.cpp`**、`**ml2mvavgs_cuda.cpp**`、`**ml2mvavgis_cuda_kernel.cu**` | 移动平均的 C++/CUDA 实现。                                      |
| `**mlgatev.cpp**`、`**lgatesd.cpp**` 等                                                          | 其他 LGate/门控相关扩展。                                        |
| `**setup.py**`                                                                                 | 各扩展的编译入口（供 `torch.utils.cpp_extension.load` 或单独编译）。     |


若只做「在 Decoder 里接 HPLSTM」的逻辑修改，通常无需改这里；若需优化性能或改 LGate/cumsum 语义，再查阅对应扩展。

---

#### 7.4.4 以 HPLSTM 为主的 Encoder/Decoder：`transformer/HPLSTM/`


| 文件                                    | 作用                                                                                                                          |
| ------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| `**Decoder.py**`                      | **Decoder-only**：每层为一个 **ResHPLSTM**（来自 `modules.hplstm.snbase`），无 attention，纯 HPLSTM 堆叠。用于传统 Seq2Seq 或非 PLM 的 HPLSTM 语言模型。 |
| `**Encoder.py`**                      | Encoder 侧为 **ResBiHPLSTM** 堆叠。                                                                                              |
| `**FNDecoder.py`**、`**FNEncoder.py`** | 使用 `**modules/hplstm/hfn.py**` 的 HPLSTM 变体（带 fhsize/GLU 等）的前馈式 Decoder/Encoder。                                             |
| `**FNLM.py**`、`**LVFNLM.py**`         | 基于上述 FNDecoder 的语言模型封装。                                                                                                     |


这里展示的是「整网由 HPLSTM 层组成」的用法；若你只是在 Qwen 的 **单层** 里加一段 HPLSTM（如注意力之后），主要参考 `**modules/hplstm/base.py`** 的接口即可，不必沿用整网结构。

---

#### 7.4.5 Decoder-only PLM 版 HPLSTM：`transformer/PLM/HPLSTM/v1/`


| 文件                | 作用                                                                                                                                                                                                                      |
| ----------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `**Decoder.py`**  | 继承 **LLMDecoder**，**nets** 为多份 **ResHPLSTM**（来自 `modules.hplstm.snbase`），即「纯 HPLSTM 堆叠的 Decoder-only LLM」。支持 **build_states**、**greedy_decode**、**beam_decode**；使用 **H2TiLentoMask**（`utils/hplstm/ilen.py`）等做长度与 mask。 |
| `**__init__.py`** | 导出该子包。                                                                                                                                                                                                                  |


若你的目标是「用纯 HPLSTM 替代 Transformer 做 Decoder-only」，可参考此目录；若只是「在 Qwen 的 Transformer 里插入 HPLSTM 块」，仍以 **7.2 + modules/hplstm/base.py** 为主。

---

#### 7.4.6 配置与训练脚本


| 路径                         | 作用                                                                                                                                                          |
| -------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `**cnfg/plm/hplstm/v1/`**  | 纯 HPLSTM PLM（`transformer/PLM/HPLSTM/v1/`）的配置：**base.py**、**ihyp.py**、**hyp.py** 等，含层数、head 数、**hplstm_i_hsize** / **hplstm_o_hsize**、是否 **use_rmsnorm** 等。 |
| `**adv/train/hplstm/lm/`** | 使用 HPLSTM 的语言模型训练入口：**train_lm.py**、**train_lm_long.py** 等，与 **train_lora_qwen.py** 无直接依赖。                                                                  |


---

#### 7.4.7 小结：按「在 Qwen 里接 HPLSTM」需要看的文件

- **接口与调用**：`**modules/hplstm/base.py`**（`HPLSTM`、`ResHPLSTM` 的 `__init__`、`forward(inpute, states=None, head_mask=None)`）。
- **状态与序列算子**：`**utils/hplstm/RS1cumsumstatnorm.py`**（states 的传入/返回）、`**utils/hplstm/LGate.py`**（理解 cell 递推）。
- **模型插入位置**：`**transformer/PLM/QWen/v3/Decoder.py`** 的 **DecoderLayer**（见 7.2）；训练与冻结逻辑：`**adv/train/plm/train_lora_qwen.py`**（见 7.1、7.3）。

### 7.5 能否用「类似 LoRA」的思路实现 HPLSTM？异同与可选改法（仅思路，本节不改代码）

「类似 LoRA」可以指两层含义：**工程流程**与**数学形式**。下面分开说；**数据管线**（`pubdatasets_to_srctgt` → map → `mkiodata_llmdec` → HDF5）与 **PMaskDataConverter** **不必**因 HPLSTM 而改，与 LoRA 相同。

---

#### 7.5.1 工程流程上：可以类比，且与当前 LoRA 脚本「并列」即可

LoRA 训练在工程上的固定套路是：

1. 加载基座权重；
2. **冻结**原参数或只保留小适配器可训练（`std2lora` 在 Linear/Embedding 上挂低秩支路）；
3. 优化器只收到 **requires_grad=True** 的参数；
4. 存 checkpoint 时常用 `**rgrad_filter`** 只存可训练部分。

HPLSTM 若作为 **Decoder 里已存在的子模块**（如 `Decoder_1` 每层 `self.hplstm`），完全可以采用 **同一套流程**，而**不必**再造一套「类 std2lora」的替换器：

- `**freeze_module(整网)`** → 再 `**unfreeze_module` 仅 `*.hplstm` 子树**（或等价地对 HPLSTM 参数 `requires_grad=True`）；  
- `**save_model_ps_func = rgrad_filter`**，与 LoRA 分支一致。

**对应实现位置（思路）**：`**adv/train/plm/train_hplstm_qwen.py`** 中与 `lora_features is None` 绑定的分支；与 `**train_lora_qwen.py`** 里 `std2lora` 分支是 **二选一** 关系，**数据与 HDF5 格式** 与 LoRA 共用同一套文档第三节。

---

#### 7.5.2 机制上：HPLSTM ≠「在 Linear 上挂低秩旁路」，与 LoRA 的 `std2lora` 不是同一类东西


| 维度        | LoRA（本项目）                                                                                                                         | HPLSTM（作为独立模块）                                                                                  |
| --------- | --------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- |
| **改什么**   | 把 **nn.Linear / nn.Embedding** 换成 **带 lora_wa/lora_wb 的层**（`**utils/lora/base.std2lora`** + `**utils/module/patcher.patch_std`**） | 在 **DecoderLayer** 里 **显式增加** `**modules/hplstm/base.HPLSTM`**（或 `ResHPLSTM`），改 **forward 数据流** |
| **预训练权重** | 原 Linear 权重从基座拷贝，LoRA 矩阵**随机初始化**                                                                                                 | 基座无 HPLSTM 时，HPLSTM **整段随机初始化**；**load_plm** 不会给 HPLSTM 填 Qwen 官方权重                             |
| **可训练参数** | 低秩矩阵 + 可选 bias/norm                                                                                                               | HPLSTM 内部 **trans_input / MHPLSTMCore / trans_output** 等全部参数（或你再手动冻结子模块）                        |


因此：**不能**把「`std2lora` 里把 `name_cfunc` 从 Linear 改成 HPLSTM」这种表述当真——`**std2lora` 只处理 Linear/Embedding 类型**，HPLSTM 是 **另一类 `nn.Module`**，需要 **结构级**插入，而不是 **类型替换**。

---

#### 7.5.3 若希望「更像 LoRA」的三种可选方向（从易到难）

1. **流程对齐（推荐先做）**
  只做 **冻结 + 按模块名解冻 HPLSTM + rgrad_filter**，不新增 patcher。  
   **涉及文件（思路）**：`**adv/train/plm/train_hplstm_qwen.py`**、`**cnfg/lora.py`**（或单独 `cnfg/hplstm.py` 存 `train_hplstm_only` 等开关）。  
   **数据格式**：不变。
2. **工程上模仿 `std2lora`：自动「插入」HPLSTM 块**
  新增类似 `**patch_hplstm_after_attn(decoder, ...)`** 的函数：遍历 `DecoderLayer`，在 `self_attn` 与 `ff` 之间注册 `HPLSTM`（若尚未存在）。语义上接近「批量改模型图」，但实现是 **插入新模块** 而非 **替换 Linear**。  
   **涉及文件（思路）**：`**utils/module/patcher.py`**（或新建 `**utils/hplstm/patch.py`**）、`**transformer/PLM/QWen/v3/Decoder.py`**、`**cnfg/plm/qwen/v3/base.py`** 或新 `**cnfg/hplstm.py**`（开关、head 数、是否用 ResHPLSTM）。  
   **注意**：`load_plm` 后插入的 HPLSTM **无预训练**；若希望从 checkpoint 续训，需单独保存/加载 HPLSTM 的 `state_dict`。
3. **数学上模仿 LoRA「旁路」：残差式 HPLSTM**
  使用 `**modules/hplstm/base.ResHPLSTM`**（或手写 `x + α * HPLSTM(x)`），使 HPLSTM 成为 可选增量，初始化时接近恒等（需调 α 或初始化策略）。**  
   **涉及文件（思路）：`**DecoderLayer.forward`**、`**Decoder_1.py**` 里当前 `context = self.hplstm(context)` 是否改为 Res 形式。

##### 7.5.3.1 前两种方法是否更像 Adapter？若「尽可能保留 LoRA 的优点、只把适配形式换成 HPLSTM」应怎么做？

- **与 Adapter 类比的边界**  
  - **7.5.3 第 1、2 种**（冻基座 + 只训插入模块 / 用 patch 批量插入 HPLSTM）在范式上 **更接近经典 Adapter**：在固定主干（预训练 Transformer）上 **外挂可训练子模块**，不动或尽量少动原权重。  
  - **LoRA** 在论文里强调的是 **对现有权重矩阵做低秩更新**（W + BA），是 **同一线性层内的参数高效改写**，不是「多插一整块新网络」。因此：**流程上** 可与 Adapter/HPLSTM-insert 共用「冻基座、只训小块」；**数学上** LoRA 的「优点」核心是 **极少参数量 + 可合并/低延迟** 等，与「一整块 HPLSTM」**不完全等价**。
- **若希望尽可能保留 LoRA 的「优点」、仅把「适配器」从 LoRA 换成 HPLSTM，可考虑的折中（思路）**  
  1. **保留 LoRA 的训练与存盘范式**：仍用 `**freeze_module` + 只解冻 HPLSTM 相关参数 + `rgrad_filter`**，数据与 HDF5 **不变**（与 7.5.1 一致）。
  2. **用「旁路 + 小系数」贴近 LoRA 的「小扰动」**：优先采用 **7.5.3 第 3 条**——`**ResHPLSTM`** 或 `out = x + α * HPLSTM(x)`，**α 取小** 或对 HPLSTM 输出做 **零初始化 / 极小初始化**，使训练初期接近原 Qwen 行为（类比 LoRA 中常把 **A 或 B 之一初始化为 0**）。
  3. **控制参数量（类比 LoRA rank）**：缩小 HPLSTM 的 **head 数、内部宽度**（`modules/hplstm/base.HPLSTM` 的 `num_head`、`i_hsize`/`o_hsize` 等），或 **只在部分层** 插 HPLSTM（例如仅后几层），避免每层满尺寸 HPLSTM 导致可训练参数量远大于典型 LoRA。
  4. **可选：LoRA 与 HPLSTM 分工**（若仍想保留 LoRA 的低秩优点）：**Linear 上仍用 `std2lora`** 只训 LoRA；HPLSTM 只做 **窄残差旁路**（小 α、浅层 HPLSTM），二者 **同时** 可训时需理清 `freeze`/`unfreeze` 顺序（见 7.5.4）。
  5. **推理侧**：LoRA 常可 **合并回权重**；HPLSTM 一般 **不能** 合并进 Linear，会多一段前向开销——若「部署时像 LoRA 一样省算力」是硬需求，需接受 **额外延迟** 或探索 **蒸馏到无 HPLSTM 模型**。

**结论**：7.5.3 前两条 **更像 Adapter 式「外挂模块」**；若要 **在单一 HPLSTM 路径下尽量贴近 LoRA 的体验**，应组合使用 **残差/缩放 + 小容量 HPLSTM + 层选择**，而不是指望 HPLSTM 在参数量上与 rank=8 的 LoRA **严格同一量级**（除非把 HPLSTM 设计得极小）。

---

#### 7.5.4 与 LoRA **同时**训练时（思路）

若既要 **LoRA** 又要 **HPLSTM** 可训练：

- **顺序**：建议 `**load_plm` → `std2lora` → 再插入/确认 HPLSTM 存在`→ 再`freeze_module`→ 再分别对 LoRA 参数与 HPLSTM 参数`requires_grad=True`**（或先 freeze 再 std2lora 再 unfreeze 两路，需避免重复` freeze` 把 LoRA 也冻掉）。  
- **优化器**：`get_model_parameters(mymodel)` 会包含所有 `requires_grad=True` 的参数；**保存**时仍可用 `**rgrad_filter`**，或拆成两个 checkpoint 分别存 LoRA 与 HPLSTM（需额外逻辑）。  
- **本节仅作设计说明**；具体实现需在训练脚本里**显式分支**并**单测**一次前向与 loss 是否反传。

---

#### 7.5.5 小结句

- **可以**用与 LoRA **相同的训练流程**（冻基座、只训小块、只存可训练参数）来训 HPLSTM；  
- **不能**用 `**std2lora` 同一套「替换 Linear」机制**直接实现 HPLSTM，除非你把 HPLSTM 再封装进某种**可 patch 的 Linear 包装**（一般不推荐）；  
- **结构**上仍要在 **DecoderLayer**（或等价 patch）里 **插入 HPLSTM**，并理清 **load_plm / 随机初始化 / 续训**。

---

